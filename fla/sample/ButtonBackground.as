package sample {

	import com.bumpslide.ui.Component;

	/**
	 * ButtonBackground Component
	 * 
	 * @author David Knape
	 */
	public class ButtonBackground extends Component 
	{

		// Layer 1		// Warning: Grouped items found 
	// (clips must be ungrouped to be identified by this JSFL script)


		/**
		 * Initialize children, bindings, and event listeners 
		 */
		override protected function addChildren() : void {
			super.addChildren();
		}


		/**
		 * Update the display 
		 */
		override protected function draw() : void {
			super.draw();
		}
	}
}